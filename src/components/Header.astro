---
const navItems = [
  { name: "Home", link: "/" },
  { name: "GitHub", link: "https://github.com/EshaanAgg/LFXMM-Frontend" },
  { name: "About", link: "/about" },
];
---

<div
  class="backdrop-blur-md sticky top-0 flex flex-col lg:flex-row justify-between items-center px-10 mb-10 border-solid border-b-2 border-highlight-blue hover:shadow-highlight-blue hover:shadow-lg duration-200 z-30"
>
  <div class="px-10 py-2">
    <div>
      <span
        class="self-center text-3xl font-semibold whitespace-nowrap text-white sm:text-5xl"
      >
        <a
          href="/"
          class="line1 animTypewriter font-header flex items-center text-center hover:text-highlight-blue duration-200 md:py-3"
          title="LFX Mentorship"
        >
          LFX Mentorship
        </a>
      </span>
    </div>
  </div>

  <div class="text-xl text-white mb-2 px-10 flex md:text-2xl">
    <ul
      class="linkUl flex flex-row justify-between items-center gap-2 md:gap-5 lg:gap-16"
    >
      {
        navItems.map((item) => (
          <li>
            <a
              href={item.link}
              class="animSlowOpacity font-subHeader hover:text-dark-blue duration-200 hover:text-3xl px-4"
            >
              {item.name}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>

<style>
  .line1 {
    border-right: 10px solid #0077cc;
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
  }
  .animTypewriter {
    animation:
      typewriter 1s steps(14) 1s 1 normal both,
      blinkTextCursor 600ms steps(14) infinite normal;
  }
  @keyframes typewriter {
    from {
      width: 0;
    }
    to {
      width: 9.1em;
    }
  }
  @keyframes blinkTextCursor {
    from {
      border-right-color: #0077cce7;
    }
    to {
      border-right-color: transparent;
    }
  }

  /* Link hover */
  .linkUl li {
    position: relative;
    cursor: pointer;
    transition: 0.5s;
  }

  .linkUl li:before {
    content: "";
    position: absolute;
    width: 100%;
    height: 100%;
    background: #9cd6ff;
    z-index: -1;
    transform: scaleY(0);
    transition: transform 0.5s;
    transform-origin: bottom;
    border-radius: 0 0 10px 10px;
  }

  .linkUl li:hover:before {
    transform: scaleY(1);
    transition: transform 0.5s;
    transform-origin: top;
  }

  .linkUl li:after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    background: #5fb7ff;
    z-index: -2;
    transform: scaleY(0);
    transition: transform 0.5s;
    transform-origin: bottom;
    filter: blur(40px);
  }

  .linkUl li:hover:after {
    transform: scaleY(1);
    transition: transform 0.5s;
    transform-origin: top;
  }

  .animSlowOpacity {
    animation-name: slowOpacity;
    animation-duration: 0.8s;
    animation-timing-function: cubic-bezier(0.95, 0.05, 0.795, 0.035);
  }

  .slowOpacityDelay1 {
    animation-duration: 1.6s;
  }

  .slowOpacityDelay2 {
    animation-duration: 2.4s;
  }

  .slowOpacityDelay3 {
    animation-duration: 3.2s;
  }

  @keyframes slowOpacity {
    from {
      opacity: 0%;
    }
    to {
      opacity: 100%;
    }
  }
</style>
